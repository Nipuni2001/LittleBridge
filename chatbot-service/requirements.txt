# ── Web framework ─────────────────────────────────────────────
flask==3.0.0
flask-cors==4.0.0
python-dotenv==1.0.0

# ── NLP / ML core ─────────────────────────────────────────────
nltk==3.8.1
numpy==1.26.4

# ── Deep Learning (choose one) ────────────────────────────────
# Option A: TensorFlow (includes Keras) — recommended for most setups
tensorflow==2.16.1

# Option B: If Option A fails on your machine, comment it out
# and use standalone Keras + TensorFlow-cpu:
# tensorflow-cpu==2.16.1

# ── Database ──────────────────────────────────────────────────
mysql-connector-python==8.2.0

# ── HTTP client (for potential backend integration) ───────────
requests==2.31.0
